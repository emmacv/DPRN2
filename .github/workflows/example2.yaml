name: marta

on:
  push:
    branches:
      - main

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: "Run a one-line script"
        run: echo "Hello, world!"
  second_job:
    runs-on: ubuntu-latest
    steps:
      - name: "Run another one-line script"
        run: echo "Hello, world!"

